void main() {
  bool error = false;
  bool noerror = true;
  //bool and = error && noerror;
  bool or = error || noerror;
  bool not = error != noerror;
  //print(and);
  print(or);
  print(not);
  
}
